{
  "name": "greeter",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "asbuild": "^0.0.11",
    "near-sdk-as": "^3.1.0"
  },
  "devDependencies": {
    "shelljs": "^0.8.4"
  },
  "scripts": {
    "build:release": "asb --verbose --target release",
    "build:debug": "asb --verbose --target debug"
  }
}
